# envelope-not-letter
An eval-awareness probe scores 0.971 AUROC on clean contrast pairs and falls to chance against length-matched controls in multi-turn transcripts. It fires on the appended context block, not its content. Olmo-3-7B-Instruct, layer 16. MATS 12.0 application.
